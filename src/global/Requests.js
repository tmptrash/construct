/**
 * Contains Id's for requests from client to server and from server to client
 *
 * @author flatline
 */

/**
 * Id's of requests from client to server
 */
const CTOS = {
    REQ_SET_ACTIVE: 0
};
/**
 * {Object} Id's of requests from server to client
 */
const STOC = {
    REQ_GIVE_ID   : 0
};

module.exports = {CTOS: CTOS, STOC: STOC};