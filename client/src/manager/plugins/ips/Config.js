/**
 * Configuration of Ips plugin
 *
 * @author flatline
 */
const Config = {
    /**
     * {Boolean} Shows/Hides IPS value on the left top corner of a canvas
     */
    show    : true,
    /**
     * {Number} Period in milliseconds, which is used to update IPS
     */
    periodMs: 1000
};

module.exports = Config;